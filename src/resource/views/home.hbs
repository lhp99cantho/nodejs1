<div class="mt-4">
    <div class="row">
        {{#each item}}
        <div class="col-sm-6 col-lg-4">
            <div class="card" style="width: 18rem; margin-top: 15px;">
                    <a href="/items/{{this.slug}}"><img src="{{this.img}}" class="card-img-top" alt="..."></a>
                <div class="card-body">
                    <a href="/items/{{this.slug}}"><h5 class="card-title">{{this.name}}</h5></a>
                    <p class="card-text">{{this.desc}}</p>
                    <a href="/items/{{this.slug}}" class="btn btn-primary">Xem chi tiết</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>